import styled from 'styled-components';
import { Typography } from 'antd';

const { Text } = Typography

export const EventModalInfoQRWrapper = styled.div`
    text-align: center;
`

export const EventModalInfoWrapper = styled.div`
`

export const BlockText = styled(Text)`
    display: block;
`